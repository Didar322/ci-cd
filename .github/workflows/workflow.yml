on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: deploy, push
    runs-on: ubuntu-latest
    steps:

    - name: Checkout main
      uses: actions/checkout@v2

    - name: Checkout main
      run: echo "deployed"